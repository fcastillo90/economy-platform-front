import { makeStyles } from '@material-ui/core/styles'

export default makeStyles({
  root: {
    minWidth: 192,
  },
  title: {
    fontSize: 14,
    maxWidth: 128,
  },
  pos: {
    marginBottom: 12,
  },
})
